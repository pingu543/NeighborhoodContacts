// The main page.
// At the top of the page is a header with branding on left and account component on right.
// If the user is an admin, an admin control component is shown below the header.
// Below the header is a list of contacts. Each contact has a address, contact name, email, and phone number.
// The contact list is paginated, showing 10 contacts per page. There are buttons to navigate to the next and previous pages.
// Clicking on a contact will open the corresponding contact details page.
// At the bottom of the page is download pdf component.
function ContactListPage() {
  return (
    <p>Hello world!</p>
  );
}

export default ContactListPage;